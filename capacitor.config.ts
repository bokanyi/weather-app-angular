import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'weather-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
